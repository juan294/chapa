# Notes for `2026-09-05-scoring-relaunch`

## Deviations

- **S01 administrative database deletion dependency:** Plan said the existing `scripts/delete-user.ts` would be extended in S14. Found the repository's exhaustive handle-column test rejects any new user-bearing schema until deletion discovery and cleanup handle it. Chose to bring the v7 database cleanup RPC, deletion inventory and its script integration into S01, with real database and script regressions. Why: the foundation must preserve working account deletion and pass existing gates; masking the schema check would leave private data behind. S14 retains the remaining receipt verification, cache revocation and retention integration.
- **S01 persistence mapping ownership:** Plan listed shared contracts and additive schema; found that separate handwritten SQL fixtures missed required shared assessment revision/provenance fields. Added `apps/web/lib/db/scoring-v7-contract.ts` as the explicit private persistence projection and proved the actual shared claim/assessment shapes round-trip through local PostgREST. Later ledger workers reuse these mappings.
- **S01 coverage scope:** The old coverage exclusion called `stats-schema.ts` a type-only module, but it now validates core inputs at runtime. Removed that exclusion and restricted shared-package coverage to `src/**`, so a local build cannot dilute coverage by adding duplicate generated `dist` files. Existing thresholds are unchanged.

- **S02/S06 worker scheduling:** The implementation workflow delegates phase implementation. The session agent quota allowed only one active child while GitHub collection was underway. The primary agent implemented the independent aggregation work while the GPT-6 Astra worker implemented GitHub, then an independent GPT-6 Astra reviewer checked both phases and performed the dedicated simplify pass. Both tasks share one fully checked integration commit; this preserves independent review without waiting for an unavailable extra worker slot.
- **S09 scheduling and private aggregation output:** Plan said S09 follows its S06 prerequisite and owns the core arithmetic entrypoint. Found the session still allowed only one active child and count derivation needed verified acceptance selections after the selected event ages out. Chose primary-agent S09 implementation in a separate engine worktree while GPT-6 Astra workers handled disjoint collector/Craft files, followed by independent Astra compliance and simplify review. Added an evidence-count helper beside the arithmetic entrypoint and an additive private `acceptanceSelections` aggregation output. Why: the shared equivalence resolver remains authoritative, later mirrored acceptances cannot rejuvenate old work, and the independent review/verification gates remain intact. No scoring policy constant or required phase was removed.

- **S10 durable report and authorization foundation:** Plan listed legacy report persistence/cache ownership. Found that immutable multi-report evidence needs atomic private storage, separate raw-body retention, and grant start-time enforcement. Chose additive migration040, a transactional report RPC, a consistent private read RPC, and raw expiry through the existing warm-cache maintenance route; future reviewer grants cannot grant access early. Why: report replay, privacy and the mandatory real-database failure matrix must work before consumer integration. S11 retains ledger authoring and reviewer history; S15 retains shared receipt caching.
- **S10 fresh aggregate projection:** Plan listed Craft cache ownership. Found that calculating from the complete database snapshot before consulting Redis cannot save that work and risks obscuring permission/expiry behavior. Chose an authorized fresh aggregate helper instead of a redundant secondary cache. Why: each read applies current authorization and the explicit scoring window; the single receipt cache remains S15's responsibility.
