## Problem and intended behavior

Use account IDs and actual merge/closure/review state events, rather than repository totals or updated_on. Collect only subject-attributed closures and reviews, including reviews on still-open changes. Preserve coverage for repository discovery, diffstats and every nested pagination call.

Epic and complete normative v7 policy: https://github.com/juan294/chapa/issues/1295. Read that policy before implementation; shared policy changes require foundation-owner review. This is task **S04**, phase **4**, worker role **backend**. All 53 findings and all 20 tasks are mandatory before relaunch.

## Dependencies

- S01: https://github.com/juan294/chapa/issues/1296

**[batch-eligible]** after S01: no dependency on another batch worker; work only in owned files against the frozen contract.

## Owned files and integration boundary

- apps/web/lib/bitbucket/queries.ts:115
- apps/web/lib/bitbucket/types.ts:46
- apps/web/lib/bitbucket/stats.ts
- apps/web/lib/bitbucket/stats-aggregation.ts
- apps/web/lib/bitbucket/*.test.ts

Adjacent tests for owned source files are included. Request shared contract/schema amendments from S01; do not concurrently change another worker's files. UI work must coordinate with the separate redesign and use the single renderBadgeSvg artifact.

## Acceptance criteria

- [ ] Another user's resolved issue supplies no subject closure credit.
- [ ] updated_on changes alone cannot move a merge/closure into the year.
- [ ] A page-two 429/error reports partial observations and retains a valid prior without silently publishing a lower complete total.
- [ ] Missing usernames still work through stable account IDs.
- [ ] Cross-provider canonical fixtures reconcile exactly where capabilities are equivalent.

## Audit requirements

- F02: Opened/repository-wide issues mislabeled as personal closures
- F15: Linked-provider event windows and timestamps inconsistent
- F23: Provider failures/diff errors become valid partials/zeros

## Implementation and review protocol

```text
read epic policy, prerequisite contracts, local plan and source
create regression/property fixtures for this task's acceptance cases
implement owned behavior with explicit reference context and coverage
independent Astra review -> fix -> review approval -> simplify
run applicable local verification sequentially
record exact tests, outputs and each requirement's acceptance artifact
stop at the phase gate; no push or deployment
```


## Sequential local verification

Run the owned adjacent tests using `pnpm exec vitest run <explicit-owned-test-paths>`; record the exact paths in the implementation report. At phase integration run sequentially: `pnpm run typecheck`, `pnpm run lint`, `pnpm run test`, and `pnpm run build`. For schema/write changes also run `pnpm run validate:migrations`, `pnpm run check:write-registration` and `pnpm run test:contract:local` against disposable local Supabase before build. Scoring changes additionally run `pnpm run test:coverage`; import changes run `pnpm run check:circular`. Do not run checks concurrently.

Final readiness also requires repository license/vulnerability and SVG/PNG/browser verification. No hosted CI or preview is a test environment. Missing prerequisites, failed checks and policy contradictions return to their owner before completion.


## Manual/domain completion

- [ ] Independent reviewer checks final behavior against the epic's exact policy and this task's counterexamples.
- [ ] Source/semantic limitations are represented in coverage and claims, not silently converted to zero or evidence of ability.
- [ ] No acceptance item is waived, represented by an unimplemented placeholder, or moved past relaunch.
- [ ] Review and validation evidence is linked before closing.

## Authorization and baseline

Planning issues are authorized; implementation awaits approval of the completed /plan. Use **GPT-6 Astra agents only**. Baseline is local develop at c1ce31cbf5969ee400c4cf232d67cf7dc1b1de0d, with locally fixed #1287/#1294 preserved as regressions. Source plan: docs/plans/2026-09-05-scoring-relaunch-phases/phase-4.md (currently local; the epic and this body are self-contained).

Use isolated worktrees and central integration. Do not push, open PRs, create Vercel previews, trigger hosted CI as experimentation, send messages/emails to others, recruit reviewers, or mutate/recompute/deploy production from this ticket. Stop after each verified phase unless continuation is explicitly authorized. Future publishing/deployment requires local gates, trigger inspection and the user's applicable explicit authorization. Ticket closure is not release authorization.
