No changes in the blueprint since the last sync. The HEAD commit (`7ef063d`) matches the `lastSyncCommit` in the sync metadata, and the version is still `v1.15.0`.

cc-rpi sync: already up to date as of v1.15.0.
