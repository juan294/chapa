cc-rpi sync: no project-level updates needed for v1.17.1 → v1.17.2. The only changes between those versions were `CHANGELOG.md` and `README.md` in the blueprint itself — no template, command, rule, skill, or `AGENTS.md`/`CLAUDE.md` section changed. Project files are effectively up to date with v1.17.2.

Note: attempted to bump `.claude/cc-rpi-sync.json` from v1.17.1 → v1.17.2 but the write was blocked by permissions (sensitive file). No commit was created. The stored `lastSyncCommit` still points at v1.17.1, but no blueprint content is behind.
