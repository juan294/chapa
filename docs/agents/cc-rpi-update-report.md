The cc-rpi HEAD is identical to the last synced commit (`a55c8a1b`, tag `v1.25.0`) — no upstream changes since 2026-07-02. Per Phase 1, nothing changed, so the sync stops here with no updates needed.

cc-rpi sync: already up to date as of v1.25.0.
