cc-rpi sync: already up to date as of v1.25.0.

The blueprint at `/Users/juan/code/cc-rpi` pulled clean ("Already up to date"), and its HEAD (`a55c8a1`) matches the `lastSyncCommit` recorded in `.claude/cc-rpi-sync.json` from the 2026-07-02 sync. No commits, no changed files, so no commands, skills, rules, CLAUDE.md sections, or settings needed updating, and no commit was made.
