/**
 * #1335 phase 5 — v6 is retired. The landing sample is the curated v7.2
 * illustrative view model (CLAUDE.md: 92 / Elite / Balanced), kept under the
 * historical name so its consumers didn't all need a rename for a value
 * swap. `LANDING_OBSERVED_DEMO` (lib/render/observed-demo-data.ts) is the
 * one place that composes it.
 */
export { LANDING_OBSERVED_DEMO as LANDING_IMPACT } from "./observed-demo-data";
