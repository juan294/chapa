import { describe, it } from "vitest";

describe("Impact v3 scoring", () => {
  it.todo("should normalize and cap values correctly");
  it.todo("should clamp confidence to [50, 100]");
  it.todo("should assign correct tier based on adjusted score");
});
