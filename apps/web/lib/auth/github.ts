/**
 * GitHub auth helpers stub (Teammate A).
 * Will be replaced with real OAuth implementation.
 */

export async function getAuthedGithubClient(): Promise<null> {
  // Stub — returns null until OAuth is implemented
  return null;
}

export async function isAuthenticated(): Promise<boolean> {
  // Stub — always false until OAuth is implemented
  return false;
}
