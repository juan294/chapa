/**
 * GitHub API client stub (Teammate B).
 * Will create authenticated or unauthenticated GitHub client.
 */

export function createGitHubClient(_token?: string): null {
  // Stub — returns null until GitHub client is implemented
  return null;
}
