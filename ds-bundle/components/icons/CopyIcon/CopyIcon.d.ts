import * as React from 'react';

/**
 * CopyIcon — from @chapa/web@2.24.1.
 */
export interface CopyIconProps {
className?: string;
}

export declare const CopyIcon: React.ComponentType<CopyIconProps>;
